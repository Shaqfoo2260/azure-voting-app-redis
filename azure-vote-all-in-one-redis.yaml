apiVersion: apps/v1
kind: Deployment
metadata:
  name: azure-vote-back-green
spec:
  selector:
    matchLabels:
      app: azure-vote-back-green
  template:
    metadata:
      labels:
        app: azure-vote-back-green
    spec:
      containers:
      - name: azure-vote-back-green
        image: gzanjenkinsregistry.azurecr.io/redis:latest
        ports:
        - containerPort: 6379
          name: redis-green
---
apiVersion: v1
kind: Service
metadata:
  name: azure-vote-back-green
spec:
  ports:
  - port: 6379
  selector:
    app: azure-vote-back-green
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: azure-vote-front-green
spec:
  selector:
    matchLabels:
      app: azure-vote-front-green
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5 
  template:
    metadata:
      labels:
        app: azure-vote-front-green
    spec:
      containers:
      - name: azure-vote-front-green
        image: ${IMAGE_TAG}
        ports:
        - containerPort: 80
        env:
        - name: REDIS
          value: "azure-vote-back-green"
---
apiVersion: v1
kind: Service
metadata:
  name: azure-vote-front-green
  annotations:
   service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  ports:
  - port: 80
  selector:
    app: azure-vote-front-green
